# Used by docker-compose.yml. Copy to .env and adjust if you want non-default
# credentials/ports; the defaults below work out of the box for local use.
DB_PASSWORD=velvra_dev_pw
JWT_SECRET=c2VjcmV0LWtleS1mb3ItdmVsdnJhLWRlbW8tYXBwLWNoYW5nZS1pbi1wcm9kdWN0aW9uLW9ubHktZm9yLWRldg==

# Host ports the app is published on. Change these if 3000 or 8080 are
# already taken on your machine - the containers still talk to each other
# over their internal ports, so nothing else needs to change.
FRONTEND_PORT=3000
BACKEND_PORT=8080
